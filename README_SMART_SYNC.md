# تطبيق كاشير صالة الألعاب مع نظام التحكم المشترك الذكي

## نظرة عامة

تطبيق كاشير متقدم لصالات الألعاب الإلكترونية يتميز بنظام تحكم مشترك ذكي يسمح بالتحكم المتزامن بأجهزة متعددة على نفس الشبكة المحلية.

## الميزات الرئيسية

### 1. إدارة الأجهزة
- ✓ تسجيل وتتبع أجهزة متعددة
- ✓ عرض حالة كل جهاز
- ✓ إدارة أسعار وأوقات التشغيل

### 2. نظام التحكم المشترك الذكي
- ✓ مزامنة فورية بين الأجهزة
- ✓ اكتشاف تلقائي للأجهزة على الشبكة
- ✓ مزامنة في الوقت الفعلي عند إجراء تغيير
- ✓ واجهة إدارة شاملة لجميع الأجهزة

### 3. إدارة الطلبات والحجوزات
- ✓ تسجيل الطلبات والحجوزات
- ✓ تتبع الأوقات والأسعار
- ✓ إضافة ملاحظات وتفاصيل

### 4. التقارير المالية
- ✓ إحصائيات يومية وشهرية
- ✓ تحليل الإيرادات والنفقات
- ✓ تقارير مفصلة للأجهزة

### 5. الأمان
- ✓ نظام ترخيص متقدم
- ✓ معرفات فريدة لكل جهاز
- ✓ تشفير البيانات الحساسة

## المتطلبات

```
Flutter: >= 3.0.0
Dart: >= 2.18.0
Android: >= 5.0 (API 21)
iOS: >= 11.0

متطلبات النظام:
- 100 MB مساحة التخزين
- اتصال WiFi للمزامنة المحلية
- RAM: 2 GB على الأقل
```

## المكتبات الرئيسية

```yaml
# واجهة المستخدم
flutter
provider: ^6.0.5
animations: ^2.0.8

# تخزين البيانات
hive: ^2.2.3
hive_flutter: ^1.1.0
shared_preferences: ^2.2.2

# المزامنة والشبكات
connectivity_plus: ^5.0.0
web_socket_channel: ^2.4.0

# الأدوات الإضافية
intl: ^0.20.2
uuid: ^4.0.0
crypto: ^3.0.3
```

## البنية المشروعية

```
lib/
├── main.dart                           # نقطة البداية
├── app_state.dart                      # إدارة الحالة العامة
├── auth_service.dart                   # نظام المصادقة
├── license_manager.dart                # إدارة الترخيص
├── printer_service.dart                # خدمة الطباعة
├── data_persistence_manager.dart       # إدارة البيانات
│
├── # نظام التحكم المشترك الذكي
├── sync_service.dart                   # خدمة المزامنة الأساسية
├── local_network_sync_service.dart     # مزامنة الشبكة المحلية
├── device_group_manager.dart           # مدير مجموعة الأجهزة
├── device_group_screen.dart            # واجهة إدارة المجموعة
├── smart_sync_config.dart              # إعدادات النظام
│
├── # الشاشات الرئيسية
├── device_grid.dart                    # شبكة عرض الأجهزة
├── device_management_screen.dart       # إدارة الأجهزة
├── orders_prices_dialog.dart           # إدارة الطلبات والأسعار
├── reservation_dialog.dart             # إدارة الحجوزات
├── settings_screen.dart                # الإعدادات
├── financial_reports_screen.dart       # التقارير المالية
├── license_activation_screen.dart      # تفعيل الترخيص
└── login_screen.dart                   # شاشة تسجيل الدخول

test/
└── sync_service_test.dart              # اختبارات المزامنة

assets/
├── fonts/                              # الخطوط المخصصة
└── sounds/                             # ملفات صوتية
```

## التثبيت والإعداد

### 1. استنساخ المشروع
```bash
cd c:\Users\HMa\cashier_app
```

### 2. تثبيت المكتبات
```bash
flutter pub get
```

### 3. بناء الملفات المولدة
```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

### 4. بناء التطبيق

**للاختبار المحلي:**
```bash
flutter run
```

**للإنتاج - استخدم ملف البناء:**
```bash
build_smart_sync.bat
```

## الاستخدام

### البدء السريع

1. **فتح التطبيق على جميع الأجهزة**
2. **تسجيل الدخول** (إذا كان مطلوباً)
3. **الذهاب إلى "إدارة مجموعة الأجهزة"**
4. **سيتم اكتشاف الأجهزة تلقائياً** ✓

### التحكم المشترك

عند التحكم بجهاز واحد:
```
تغيير على جهاز واحد
↓
بث التحديث للشبكة المحلية
↓
استقبال جميع الأجهزة الأخرى للتحديث
↓
تحديث تلقائي لجميع الأجهزة ✓
```

### مثال في الكود

```dart
// في أي مكان في التطبيق
final groupManager = context.read<DeviceGroupManager>();

// عند إجراء تغيير
deviceData.isRunning = !deviceData.isRunning;

// بث التحديث لجميع الأجهزة
await groupManager.broadcastDeviceState('Device-1', deviceData);

// سيتم تحديث جميع الأجهزة تلقائياً!
```

## التكوين والإعدادات

### ملف الإعدادات: `lib/smart_sync_config.dart`

```dart
// تخصيص الإعدادات
SmartSyncConfig.discoveryPort = 5555;
SmartSyncConfig.defaultSyncIntervalSeconds = 5;
SmartSyncConfig.maxDevicesPerGroup = 50;
```

### المنافذ المستخدمة
- **5555**: بث اكتشاف الأجهزة
- **5556**: مزامنة البيانات

## الاختبار

### تشغيل الاختبارات الوحدة
```bash
flutter test
```

### اختبار يدوي مع أجهزة متعددة
```bash
# أولاً: شغّل على جهاز أول
flutter run

# ثانياً: في نافذة أخرى، شغّل على جهاز ثاني
flutter run -d <device-id>

# قم بتغيير شيء على أحدهما
# تحقق من تحديث الآخر فوراً!
```

## التوثيق الإضافية

- [QUICK_START.md](QUICK_START.md) - البدء السريع (5 دقائق)
- [SMART_SYNC_DOCUMENTATION.md](SMART_SYNC_DOCUMENTATION.md) - التوثيق الكامل
- [SMART_SYNC_USER_GUIDE.md](SMART_SYNC_USER_GUIDE.md) - دليل المستخدم

## استكشاف الأخطاء

### الأجهزة لا تظهر؟
```bash
1. تأكد من الاتصال بنفس WiFi
2. تحقق من تفعيل UDP على الجدار الناري
3. أعد تشغيل جهاز التوجيه
4. شغّل: flutter clean && flutter pub get
```

### التحديثات لا تظهر؟
```dart
// تحقق من تفعيل المزامنة
groupManager.enableAutoSync();

// تحقق من الفترة
groupManager.setSyncInterval(5);

// اعرض السجلات
print(groupManager.exportSyncLogs());
```

### أداء بطيء؟
```dart
// قلل عدد الأجهزة
// أو زد فترة المزامنة
groupManager.setSyncInterval(10);

// أو عطّل المزامنة التلقائية مؤقتاً
groupManager.disableAutoSync();
```

## الأداء والموارد

### متوسط استهلاك الموارد
- **CPU**: < 5% في الوضع العادي
- **الذاكرة**: 50-100 MB
- **البطارية**: ~ 2% لكل ساعة من المزامنة
- **الشبكة**: 1-5 KB/ثانية

### الحدود الموصى بها
- **عدد الأجهزة**: 5-50 جهاز
- **فترة المزامنة**: 1-30 ثانية
- **حجم التحديث**: < 64 KB

## الأمان

### ميزات الأمان
- ✓ معرفات فريدة (UUID) لكل جهاز
- ✓ تحقق من صحة البيانات
- ✓ تشفير البيانات الحساسة
- ✓ سجل مفصل لجميع التحديثات
- ✓ نظام ترخيص متقدم

### أفضل الممارسات
1. استخدم شبكة WiFi خاصة
2. قم بتحديث التطبيق بانتظام
3. احفظ النسخ الاحتياطية من البيانات
4. راقب سجلات المزامنة بانتظام

## التطوير والتوسع

### إضافة ميزة جديدة

```dart
// 1. أنشئ نموذج البيانات
class NewFeatureData {
  // البيانات الخاصة بك
}

// 2. أضف معالج في DeviceGroupManager
Future<void> broadcastNewFeature(NewFeatureData data) async {
  // كودك هنا
}

// 3. استخدمها في الواجهة
await groupManager.broadcastNewFeature(data);
```

### إضافة واجهة جديدة

```dart
// 1. أنشئ ملف الواجهة
class NewScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Consumer<DeviceGroupManager>(
      builder: (context, groupManager, _) {
        // بنائك هنا
      },
    );
  }
}

// 2. أضفها إلى الملاحة
Navigator.push(
  context,
  MaterialPageRoute(builder: (_) => NewScreen()),
);
```

## المساهمة والتحسينات المستقبلية

### المخطط المستقبلي
- [ ] دعم المزامنة السحابية
- [ ] تطبيق ويب للمراقبة
- [ ] نظام الإخطارات المتقدم
- [ ] تقارير أكثر تفصيلاً
- [ ] دعم متعدد اللغات محسّن

## الدعم والمساعدة

### قنوات الدعم
1. اقرأ الملفات التوثيقية المرفقة
2. تحقق من السجلات: `flutter logs`
3. استخدم الوضع المفصل: `flutter run --verbose`

## الترخيص والملكية

جميع الحقوق محفوظة لصالة ألعاب برلين

---

## ملخص الملفات الجديدة

| الملف | الوصف |
|------|-------|
| `sync_service.dart` | خدمة المزامنة الأساسية |
| `local_network_sync_service.dart` | مزامنة الشبكة المحلية UDP |
| `device_group_manager.dart` | مدير مجموعة الأجهزة |
| `device_group_screen.dart` | واجهة إدارة المجموعة |
| `smart_sync_config.dart` | إعدادات النظام |
| `test/sync_service_test.dart` | الاختبارات |
| `QUICK_START.md` | البدء السريع |
| `SMART_SYNC_DOCUMENTATION.md` | التوثيق الكامل |
| `SMART_SYNC_USER_GUIDE.md` | دليل المستخدم |
| `build_smart_sync.bat` | ملف بناء التطبيق |

---

**تم إنشاؤه:** يناير 2026
**الإصدار:** 1.1.0
**الحالة:** جاهز للاستخدام الإنتاجي ✓

استمتع بنظام التحكم المشترك الذكي! 🚀
