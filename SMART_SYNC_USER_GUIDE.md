# تعليمات استخدام نظام التحكم المشترك الذكي

## المتطلبات
- Flutter 3.0 أو أحدث
- Dart 2.18 أو أحدث
- أجهزة محمولة على نفس الشبكة المحلية (WiFi)

## الخطوة 1: تثبيت المكتبات
```bash
flutter pub get
```

## الخطوة 2: بناء التطبيق

### خيار أول: استخدام ملف البناء (الموصى به)
```bash
build_smart_sync.bat
```

### خيار ثاني: بناء يدوي

**لـ Android:**
```bash
flutter build apk --release
```

**لـ iOS:**
```bash
flutter build ios --release
```

## الخطوة 3: نشر التطبيق على الأجهزة

### 1. تثبيت على جهاز واحد:
```bash
flutter install
```

### 2. تثبيت متعدد على عدة أجهزة:
```bash
# تثبيت على الجهاز الأول
adb install -r build\app\outputs\flutter-apk\app-release.apk

# تثبيت على الجهاز الثاني
adb -s <device-id> install -r build\app\outputs\flutter-apk\app-release.apk
```

## الخطوة 4: إعداد الشبكة

### تأكد من:
1. جميع الأجهزة متصلة بنفس شبكة WiFi
2. الجدار الناري لا يحجب المنافذ 5555 و 5556
3. الأجهزة قريبة بشكل كافٍ لاستقبال الإشارة

## الخطوة 5: تشغيل التطبيق

### على كل جهاز:
1. افتح التطبيق
2. سجل الدخول (إذا لزم الأمر)
3. انتقل إلى "إدارة مجموعة الأجهزة"
4. تحقق من ظهور الأجهزة الأخرى في القائمة

## الاستخدام الأساسي

### تفعيل التحكم المشترك:

1. **من أي جهاز:**
   - قم بإجراء تغيير (مثل تشغيل/إيقاف جهاز لعب)
   - سيتم تحديث جميع الأجهزة الأخرى تلقائياً

2. **مثال عملي:**
   ```
   الجهاز 1: شغّل الجهاز رقم 5
   ↓
   يتم البث إلى شبكة WiFi
   ↓
   الجهاز 2 و 3: تحديث تلقائي للجهاز 5 (مشغل)
   ```

### مراقبة الأجهزة:

من شاشة "إدارة مجموعة الأجهزة" يمكنك:
- رؤية جميع الأجهزة المتصلة
- حالة كل جهاز (متصل/منقطع)
- آخر وقت تحديث لكل جهاز
- عدد الأجهزة المتصلة

### إعدادات المزامنة:

1. اضغط على أيقونة الإعدادات ⚙️
2. فعّل/عطّل المزامنة التلقائية
3. غير فترة المزامنة (1-30 ثانية)

### عرض السجلات:

1. اضغط على أيقونة السجل 📋
2. شاهد جميع حركات المزامنة
3. اضغط "تصدير" لحفظ السجلات

## استكشاف الأخطاء

### الأجهزة لا تظهر في القائمة:

**الحل:**
1. تأكد من اتصال جميع الأجهزة بنفس WiFi
2. أعد تشغيل تطبيق واحد على الأقل
3. انتظر 10 ثواني لبدء عملية الاكتشاف
4. تحقق من إعدادات الجدار الناري

### التحديثات لا تظهر على الأجهزة الأخرى:

**الحل:**
1. تأكد من تفعيل المزامنة التلقائية
2. تحقق من أن فترة المزامنة معقولة (ليست كبيرة جداً)
3. تحقق من اتصال WiFi على جميع الأجهزة
4. أعد تشغيل التطبيق على الأجهزة

### أداء بطيء:

**الحل:**
1. قلل عدد الأجهزة إذا كانت أكثر من 50
2. زد فترة المزامنة (قد تكون عالية جداً)
3. تحقق من قوة إشارة WiFi
4. أعد تشغيل جهاز التوجيه

## الحالات الخاصة

### مزامنة جهاز محدد فقط:

إذا أردت مزامنة تحديث مع جهاز معين فقط:
```dart
final groupManager = context.read<DeviceGroupManager>();
await groupManager.syncWithDevice(targetDeviceId, deviceData);
```

### إيقاف المزامنة مؤقتاً:

```dart
groupManager.disableAutoSync();
// قم بإجراءاتك
groupManager.enableAutoSync();
```

### الحصول على حالة المجموعة:

```dart
final state = groupManager.getGroupState();
print('عدد الأجهزة: ${state['deviceCount']}');
print('حالة المزامنة: ${state['syncStatus']}');
```

## الأداء والموارد

### استهلاك البطارية:
- المزامنة التلقائية تستهلك بطارية قليلة جداً
- الاكتشاف يعمل في الخلفية

### استهلاك الشبكة:
- بث الاكتشاف: ~100 بايت كل 10 ثواني
- بث التحديث: بحسب حجم البيانات (عادة < 1 KB)

### الذاكرة:
- المتطلبات الإضافية منخفضة جداً (~5-10 MB)

## الأمان

### ملاحظات أمان:
1. النظام يعمل على الشبكة المحلية فقط
2. لا توجد بيانات حساسة مرسلة عبر البث
3. معرف الجهاز فريد لكل جهاز
4. يمكنك إضافة تشفير إضافي إذا لزم الأمر

## نصائح للاستخدام الأمثل

1. **الشبكة المستقرة:**
   - استخدم شبكة WiFi مستقرة وسريعة
   - تجنب الشبكات العامة

2. **فترة المزامنة:**
   - للتطبيقات الفعلية الحية: 1-2 ثانية
   - للتطبيقات العادية: 5-10 ثواني
   - للتطبيقات التي لا تتطلب سرعة عالية: 15-30 ثانية

3. **حد أقصى للأجهزة:**
   - اختبر مع 5-10 أجهزة أولاً
   - يمكن الوصول إلى 50+ جهاز مع ضبط مناسب

4. **المراقبة:**
   - تحقق من سجلات المزامنة بانتظام
   - راقب حالة الاتصال من شاشة الإدارة

## التطوير والتخصيص

### تخصيص رسالة البث:

في `sync_service.dart`:
```dart
void broadcastDeviceUpdate(String deviceName, DeviceData deviceData) {
  // أضف منطقك الخاص هنا
}
```

### إضافة معالجات أحداث مخصصة:

```dart
syncService.syncEvents.listen((event) {
  if (event.type == 'device_update') {
    // معالجة مخصصة
  }
});
```

## الدعم والمساعدة

### المشاكل الشائعة:

| المشكلة | السبب المحتمل | الحل |
|-------|----------|-----|
| لا تظهر أجهزة | WiFi مختلفة | تحقق من WiFi |
| تحديثات بطيئة | فترة مزامنة طويلة | قلل الفترة |
| أداء بطيء | عدد أجهزة كثير | قلل عدد الأجهزة |
| انقطاعات متكررة | إشارة ضعيفة | اقترب من جهاز التوجيه |

### السجلات والتصحيح:

```bash
# عرض السجلات في الوقت الفعلي
flutter logs

# البحث عن رسائل معينة
flutter logs | findstr "Sync"
```

---

استمتع بنظام التحكم المشترك الذكي! 🚀
