# البدء السريع - نظام التحكم المشترك الذكي

## في 5 دقائق فقط

### الخطوة 1: التحضير (دقيقة 1)
```bash
# انتقل لمجلد المشروع
cd c:\Users\HMa\cashier_app

# حمّل المكتبات
flutter pub get
```

### الخطوة 2: البناء (دقائق 2-3)
```bash
# اختر إحدى الطرق:

# الطريقة الأولى: استخدم الملف المعد مسبقاً
build_smart_sync.bat

# الطريقة الثانية: بناء يدوي
flutter build apk --release
```

### الخطوة 3: التثبيت (دقيقة 4)
```bash
# تثبيت على جهاز واحد
flutter install

# أو على أجهزة متعددة
adb install -r build\app\outputs\flutter-apk\app-release.apk
```

### الخطوة 4: التشغيل (دقيقة 5)
1. افتح التطبيق على جميع الأجهزة
2. انتقل إلى "إدارة مجموعة الأجهزة"
3. سيتم اكتشاف الأجهزة تلقائياً ✓

---

## خريطة الملفات الجديدة

```
lib/
├── sync_service.dart              ← خدمة المزامنة الأساسية
├── local_network_sync_service.dart ← مزامنة الشبكة المحلية
├── device_group_manager.dart      ← إدارة مجموعة الأجهزة
└── device_group_screen.dart       ← واجهة المستخدم

الملفات التوثيقية:
├── SMART_SYNC_DOCUMENTATION.md     ← التوثيق الكامل
├── SMART_SYNC_USER_GUIDE.md        ← دليل المستخدم
├── build_smart_sync.bat            ← ملف البناء
└── QUICK_START.md                  ← هذا الملف
```

---

## الاستخدام الفوري

### قم بتشغيل الميزة:

**في main.dart:**
```dart
// أضف الاستيراد
import 'device_group_manager.dart';

void main() async {
  // ... الكود الموجود ...
  
  // هيّئ مدير المجموعة (إضافي)
  final groupManager = DeviceGroupManager();
  
  runApp(MyApp());
}
```

**في شاشة الإعدادات أو الملاحة:**
```dart
ListTile(
  title: const Text('إدارة مجموعة الأجهزة'),
  icon: const Icon(Icons.devices),
  onTap: () {
    Navigator.of(context).push(
      MaterialPageRoute(
        builder: (context) => const DeviceGroupScreen(),
      ),
    );
  },
)
```

---

## اختبار سريع

### 1. اختبار بجهاز واحد:
```bash
# شغّل التطبيق في وضع تطوير
flutter run

# انتقل إلى شاشة إدارة المجموعة
# ستظهر بيانات الجهاز تلقائياً
```

### 2. اختبار بجهازين:
```bash
# على الجهاز الأول
flutter run

# على الجهاز الثاني (في نافذة أخرى)
flutter run -d <device-id>

# قم بتغيير شيء على جهاز واحد
# سيتحدث الجهاز الآخر تلقائياً! ✓
```

---

## الميزات الرئيسية

| الميزة | الوصف |
|-------|-------|
| 🔄 المزامنة التلقائية | تحديث فوري لجميع الأجهزة |
| 🌐 اكتشاف تلقائي | يجد الأجهزة تلقائياً على الشبكة |
| 📊 لوحة إدارة | مراقبة جميع الأجهزة من واجهة واحدة |
| 📈 السجلات | تتبع كامل لجميع التحديثات |
| ⚙️ قابل للتخصيص | اضبط فترة المزامنة والإعدادات |
| 🛡️ آمن | معرفات فريدة وتحقق من الصحة |

---

## معالجة المشاكل

### الأجهزة لا تظهر؟
```bash
# 1. تحقق من الاتصال
ipconfig

# 2. أعد تشغيل الجهاز الموجه
# 3. تأكد من نفس الشبكة على جميع الأجهزة
```

### التحديثات بطيئة؟
```dart
// قلل فترة المزامنة
groupManager.setSyncInterval(2); // 2 ثانية بدلاً من 5
```

### تحديثات لا تظهر؟
```dart
// تأكد من تفعيل المزامنة
groupManager.enableAutoSync();
```

---

## الخطوة التالية

1. **اقرأ التوثيق الكامل:** [SMART_SYNC_DOCUMENTATION.md](SMART_SYNC_DOCUMENTATION.md)
2. **دليل المستخدم المفصل:** [SMART_SYNC_USER_GUIDE.md](SMART_SYNC_USER_GUIDE.md)
3. **استكشاف الأخطاء:** انظر القسم الخاص به في دليل المستخدم

---

## ملاحظات مهمة

⚠️ **المتطلبات:**
- Flutter 3.0+
- Dart 2.18+
- نفس الشبكة المحلية (WiFi)
- منافذ 5555 و 5556 متاحة

✅ **التحسينات الموصى بها:**
- استخدم شبكة WiFi 5GHz للسرعة الأعلى
- ضع الأجهزة على مسافة معقولة من جهاز التوجيه
- استخدم 5-50 جهاز للأداء الأمثل

---

## المساعدة

### عرض السجلات الحية:
```bash
flutter logs
```

### الوضع التطويري:
```bash
flutter run --verbose
```

### إعادة تشغيل كاملة:
```bash
flutter clean
flutter pub get
flutter run
```

---

**استمتع! 🎮✨**
